export interface DataTypes {
  cache: boolean;
  cookies: boolean;
  history: boolean;
  downloads: boolean;
  form_data: boolean;
  passwords: boolean;
  local_storage: boolean;
  indexed_db: boolean;
  service_workers: boolean;
  file_systems: boolean;
  plugin_data: boolean;
  web_sql: boolean;
}
